import React from "react";

const WorkExperienceForm = () => (
    <div className="w-full bg-white rounded-xl flex flex-wrap p-6 flex flex-wrap">
        <span className="w-full text-2xl font-bold text-gray-700">Add New Experience</span>
        
        <form>
            <label></label>
        </form>
    </div>
);

export default WorkExperienceForm;
